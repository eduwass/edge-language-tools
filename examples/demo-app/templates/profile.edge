{{--
@types {
  user: { displayName: string, bio: string }
  activePage: string
}
--}}
@include('partials/nav')

@userCard({ displayName: user.displayName, bio: user.bio })
  @slot('actions')
    <button>Follow</button>
  @end
@end
