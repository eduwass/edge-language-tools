---
title: Dropdown Menu
description: Dropdown menu with trigger and items; requires basecoat dropdown-menu JS (all.min.js).
---

Dropdown menu with trigger and items; requires basecoat dropdown-menu JS (all.min.js).

## Preview

<iframe src="/previews/dropdown_menu.html" title="Dropdown Menu preview" loading="lazy" style={{ width: '100%', minHeight: '180px', border: '1px solid var(--color-border-tertiary)', borderRadius: '8px' }} />

<Expandable title="Source">

```edge
@dropdownMenu({
  id: 'preview-dropdown',
  items: [
    { label: 'Profile', type: 'item' },
    { label: 'Settings', type: 'item' },
    { type: 'separator' },
    { label: 'Logout', type: 'item' },
  ],
})
  @slot('trigger')
    Open menu
  @end
@end
```

</Expandable>


## Props

<TypeTable
  type={{
    id: { type: "string", required: true },
    items: { type: "Array<{\n    type?: 'item' | 'group' | 'separator'\n    label?: string\n    url?: string\n    icon?: string\n    items?: Array<{\n      type?: 'item' | 'group' | 'separator'\n      label?: string\n      url?: string\n      icon?: string\n      attrs?: Record<string, string>\n    }>\n    attrs?: Record<string, string>\n    id?: string\n  }>" },
    trigger_attrs: { type: "Record<string, string>" },
    main_attrs: { type: "Record<string, string>" }
  }}
/>

Plus any additional HTML attributes, forwarded to the root element.

