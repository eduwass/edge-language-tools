{{--
@name Table
@desc Styled data table wrapper.
@types {
  // Additional HTML attributes forwarded to the outer .table wrapper div.
  [attr: string]: unknown
}
--}}
<div {{ html.attrs({ class: 'table' }) }}>
  <table>
    {{{ await $slots.main() }}}
  </table>
</div>
