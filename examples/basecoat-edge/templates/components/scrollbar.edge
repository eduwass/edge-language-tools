{{--
@name Scrollbar
@desc Scrollable region with custom scrollbar styling.
@types {
  [attr: string]: unknown
}
--}}
<div {{ html.attrs({ class: 'scrollbar' }) }}>
  {{{ await $slots.main() }}}
</div>
